# Bajaj Webhook Application Configuration
spring.application.name=bajaj-webhook-app

# Server configuration
server.port=8080

# Logging configuration
logging.level.com.bajaj.webhook=INFO
logging.level.org.springframework.web.client=DEBUG

# Application information
app.name=Bajaj Webhook Application
app.version=1.0.0
app.description=Spring Boot application for webhook and SQL problem solving